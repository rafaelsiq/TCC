import UserContent from "../../../global/UserFile/UserContent";
import Streamer from "../../../global/StreamerFile/Streamer";

const data : UserContent = {
    name: 'Rafael',
    login: true
} 

export default new Streamer(data);  