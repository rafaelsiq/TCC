
export const passwordReg = /(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{6,}/;
export default {
    createAds: {
        body: {
        
        },
    },
    updateAd: {
        body: {
        
        },
    },
};